<?xml version="1.0" encoding="UTF-8"?>
<local:BaseContentPage
    xmlns="http://xamarin.com/schemas/2014/forms"
    xmlns:x="http://schemas.microsoft.com/winfx/2009/xaml"
    xmlns:local="clr-namespace: Microsoft.MobCAT.Forms.Pages"
    xmlns:vm="clr-namespace:MobCATShell.Forms.ViewModels"
    x:Class="MobCATShell.Forms.Views.NestedBPage"
    x:TypeArguments="vm:NestedBViewModel">
    <ContentPage.Content>
        <StackLayout
            HorizontalOptions="Center"
            VerticalOptions="Center">
            <Label
                Text="This is the B page" />
            <Button
                Text="Navigate to details page"
                Command="{Binding NavigateToDetailsCommand}" />
            <Button
                Text="Route to details page"
                Command="{Binding RouteToADetailsCommand}" />
            <Button
                Text="Dismiss"
                Command="{Binding DismissCommand}" />
        </StackLayout>
    </ContentPage.Content>
</local:BaseContentPage>